package com.my_hourly.leave.enums;

/**
 * All supported leave types use a YEARLY allocation model.
 * Annual leave is allocated once per year (e.g. on Jan 1st) per employee.
 */
public enum LeaveAllocationType {

    YEARLY

}